# ============================================================
#  知乎搜索 API 环境变量示例
#  使用：复制本文件为 .env，然后填入实际值
#    Windows:  copy .env.example .env
#    Linux/Mac: cp .env.example .env
# ============================================================

# 知乎 Cookie（必填）
# 获取方法：浏览器登录知乎后，打开开发者工具(F12) → Network，
# 刷新任意 zhihu.com 页面，在请求头里复制完整的 Cookie 字符串。
ZHIHU_COOKIE=这里填入你的知乎Cookie

# Chrome / Chromium 可执行文件路径（必填，除非项目目录下已存在 .local-chromium）
# Windows 示例：
#   CHROMIUM_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe
# Linux 示例（Docker 内已默认设置为 /usr/bin/chromium）：
#   CHROMIUM_PATH=/usr/bin/chromium
CHROMIUM_PATH=

# 访问令牌（可选）
# 留空 = 不启用认证；填写后，访问所有 /api/* 接口都需携带：
#   Authorization: Bearer <此处填写的令牌>
# 前端「设置」页需输入同一个值。
API_TOKEN=
